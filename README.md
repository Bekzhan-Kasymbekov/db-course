Lab #3

Created a database. 
Then created a basic table, populated it, created a query through scripts with .sql extension and using psql through the terminal
 
Lab #4 

Used the SELECT, WHERE, ORDER BY and LIMIT functions on the table 'students'.

Lab #5

Used CREATE and DROP DATABASE through a script. 
Created a script to switch databases.
